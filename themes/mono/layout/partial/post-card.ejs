<article class="post-card">
  <h2 class="post-card-title">
    <a href="<%- url_for(post.path) %>"><%= post.title || '(Untitled)' %></a>
  </h2>
  <div class="post-card-meta">
    <time datetime="<%- date_xml(post.date) %>"><%- date(post.date, 'YYYY-MM-DD') %></time>
    <% if (post.categories && post.categories.length) { %>
      <span class="separator">&middot;</span>
      <% post.categories.each(function(cat){ %>
        <a class="meta-link" href="<%- url_for(cat.path) %>"><%= cat.name %></a>
      <% }) %>
    <% } %>
    <% if (theme.cards && theme.cards.word_count) { %>
      <span class="separator">&middot;</span>
      <span><% var pt = strip_html(post.content); var cn = (pt.match(/[\u4e00-\u9fff\u3400-\u4dbf\uf900-\ufaff]/g)||[]).length; %><%= cn %></span>
      <span class="separator">&middot;</span>
      <span>约 <%= Math.max(1, Math.round(cn / 350)) %> 分钟</span>
    <% } %>
  </div>
  <div class="post-card-excerpt">
    <% if (post.excerpt) { %>
      <%- post.excerpt %>
    <% } else { %>
      <p><%- strip_html(post.content).substring(0, 160) %>…</p>
    <% } %>
  </div>
  <% if (post.tags && post.tags.length) { %>
    <div class="post-card-tags">
      <% post.tags.each(function(tag){ %>
        <a href="<%- url_for(tag.path) %>">#<%= tag.name %></a>
      <% }) %>
    </div>
  <% } %>
</article>
